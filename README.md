# IRC-BOTPY 1.0

This little client is developed in python3 and have few functions for windows.


REQUIRIMENTS:

  -requests
  -secrets
  -pywifi
  -socket
  -scapy
  -time
  -sys
  -os

Don't worry about add only functions because I had imported only these


FUNCTIONS OF BOT

  -poweroff(powershell)
  -reboot(powershell)
  
  

  -startup(powershell)
  -ddos udp/tcp/icmp/http(scapy) with timer
  -donwload & execute(requests)
  -see web page[ie/firefox/chrome ecc..]
  
  -if the client is offline try each two minutes to connect of server irc
  

Remember that is the first version of irc-botpy then don't talk bad about this if don't like it.
